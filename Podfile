# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

target 'FreeCell' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FreeCell
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  target 'FreeCellTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FreeCellUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
